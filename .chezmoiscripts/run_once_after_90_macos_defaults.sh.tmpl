#!/usr/bin/env bash
# ------------------------------------------------------------------------------
# run_once_after_90_macos_defaults.sh.tmpl
# macOS 系统底层偏好设置自动化脚本 (仅在 Darwin 平台上触发执行)
# ------------------------------------------------------------------------------

{{- if .is_mac }}
set -euo pipefail

echo "🍏  正在应用 macOS 优化版系统偏好..."

# 调优键盘重复响应速率与延迟 (针对高频编码优化)
defaults write NSGlobalDomain KeyRepeat -int 2
defaults write NSGlobalDomain InitialKeyRepeat -int 15

# Finder 访达: 强制显示所有文件扩展名与 POSIX 路径栏
defaults write com.apple.finder AppleShowAllExtensions -bool true
defaults write com.apple.finder ShowPathbar -bool true

# 阻止 Finder 在网络共享盘或外接 USB 闪存盘上自动生成 .DS_Store 文件
defaults write com.apple.desktopservices DSDontWriteNetworkStores -bool true
defaults write com.apple.desktopservices DSDontWriteUSBStores -bool true

echo "✅  macOS 系统偏好优化设置完成。"
{{- else }}
# 非 macOS 平台: 自动跳过 macOS 系统偏好应用
{{- end }}
