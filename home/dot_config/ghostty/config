# ------------------------------------------------------------------------------
# Ghostty macOS GPU 加速原生终端模拟器配置文件
# Cmd 快捷键与 global 前缀均属于 macOS 宿主层；终端内 Alt 由左 Option 提供。
# ------------------------------------------------------------------------------

# 1. 外观样式与色彩主题
# Always use the dark palette, independent of the macOS appearance setting.
theme = "Catppuccin Mocha"
# Three independent layers keep code, private-use icons, and CJK fallback
# diagnosable without patching the licensed MonoLisa font files.
font-family = "MonoLisaCode Variable"
font-family = "Symbols Nerd Font Mono"
font-family = "PingFang SC"
font-style = "Regular"
font-style-bold = "Bold"
font-style-italic = "Italic"
font-style-bold-italic = "Bold Italic"
font-synthetic-style = false
font-size = 14
font-thicken = false
# Keep MonoLisa defaults intact while explicitly disabling discretionary ligatures.
font-feature = ["-dlig"]
minimum-contrast = 3

# 玻璃透明度与阴影效果
background-opacity = 0.96
background-blur-radius = 12

window-padding-x = 14
window-padding-y = 14
window-decoration = true
window-title-font-family = "Inter"
unfocused-split-opacity = 0.75

# 2. 窗口行为与光标样式
macos-titlebar-style = transparent
window-height = 42
window-width = 135
cursor-style = block
cursor-style-blink = false

# 左 Option 作为 Alt/Meta，保留 Zsh 的单词级移动。
macos-option-as-alt = left

# 3. 鼠标交互与剪贴板
focus-follows-mouse = true
mouse-hide-while-typing = true
scrollback-limit = 100000
copy-on-select = true
clipboard-read = ask
clipboard-write = allow
confirm-close-surface = true

# 4. 后台任务完成系统通知 (运行 >15s 且非当前窗口焦点时弹窗通知)
notify-on-command-finish = unfocused
notify-on-command-finish-after = 15s
notify-on-command-finish-action = notify

# 5. macOS 全局下拉快捷终端 (Quake Mode: Cmd+左 Option+Space)
keybind = global:cmd+alt+space=toggle_quick_terminal

# 6. macOS 分屏管理与 Vim 风格焦点切换快捷键
keybind = cmd+d=new_split:right
keybind = cmd+shift+d=new_split:down
keybind = cmd+h=goto_split:left
keybind = cmd+l=goto_split:right
keybind = cmd+k=goto_split:up
keybind = cmd+j=goto_split:down
keybind = cmd+z=toggle_split_zoom
